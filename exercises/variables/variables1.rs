// variables1.rs
// Make me compile!
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a hint.

fn main() {
    let x = 5;
    println!("x has the value {}", x);
    let mut y = 10;
    println!("mutable y has the value {}", y);
    y = 11;
    println!("mutable y now has the value {}", y);
}
